parameters:
  kapitan:
    compile:
      - input_paths:
          - {{ cookiecutter.component }}/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - {{ cookiecutter.component }}/component/main.jsonnet
        input_type: jsonnet
        output_path: {{ cookiecutter.component }}/
