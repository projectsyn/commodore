parameters:
  kapitan:
    compile:
      - input_paths:
          - ${_base_directory}/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - ${_base_directory}/component/main.jsonnet
        input_type: jsonnet
        output_path: {{ cookiecutter.slug }}/
{% if cookiecutter.add_pp == "y" %}
  commodore:
    postprocess:
      filters: []
{% endif -%}
